namespace CatanRoguelike.Core.Progression
{
    /// <summary>Permanent unlock ids stored in meta.json (separate from run save).</summary>
    public enum MetaUnlockId
    {
        MediumMap,
        LargeMap,
        LeaderWarlord,
        LeaderArchitect,
        ExtraDraftPick,
        StartBonusWheat,
        StartBonusCard
    }
}
